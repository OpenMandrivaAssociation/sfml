diff -urN SFML-1.6/src/SFML/Graphics/ImageLoader.cpp SFML-1.6-patched/src/SFML/Graphics/ImageLoader.cpp
--- SFML-1.6/src/SFML/Graphics/ImageLoader.cpp	2010-01-28 00:00:05.000000000 +1000
+++ SFML-1.6-patched/src/SFML/Graphics/ImageLoader.cpp	2012-01-13 20:06:57.525101193 +1100
@@ -44,7 +44,11 @@
     void PngErrorHandler(png_structp Png, png_const_charp Message)
     {
         std::cerr << "Failed to write PNG image. Reason : " << Message << std::endl;
-        longjmp(Png->jmpbuf, 1);
+#if PNG_LIBPNG_VER_MAJOR >= 1 && PNG_LIBPNG_VER_MINOR >= 4
+	longjmp(png_jmpbuf(Png), 1);
+#else
+	longjmp(Png->jmpbuf, 1);
+#endif
     }
 }
 
@@ -289,7 +293,11 @@
     }
 
     // For proper error handling...
-    if (setjmp(Png->jmpbuf))
+#if PNG_LIBPNG_VER_MAJOR >= 1 && PNG_LIBPNG_VER_MINOR >= 4
+    if (setjmp(png_jmpbuf(Png)))
+#else
+    if (setjmp(Png->jmpbuf))
+#endif
     {
         png_destroy_write_struct(&Png, &PngInfo);
         return false;
